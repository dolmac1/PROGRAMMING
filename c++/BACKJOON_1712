#include <iostream>
#include <string>
using namespace std;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int a, b, c;
	cin >> a >> b >> c;
	int count = 0;
	if (b >= c) {
		count = -1;
	}
	else {
		count = a / (c - b) + 1;
	}
	cout << count << endl;
	return 0;
}
