#include <iostream>
#include <string>
using namespace std;
int factorial(int a) {
	if (a <= 1) {
		return 1;
	}
	else {
		return a*factorial(a - 1);
	}
}
int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int i;
	cin >> i;
	int result = factorial(i);
	cout << result << endl;
	return 0;
}
