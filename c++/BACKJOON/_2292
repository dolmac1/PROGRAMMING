#include <iostream>
#include <string>
using namespace std;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int a;
	cin >> a;
	int h = 1;
	int k = 0;
	while (1) {
		if (a > h) {
			h = h + (6 * (k + 1));
			k++;
		}
		else {
			k++;
			break;
		}
	}
	cout << k << endl;
	return 0;
}
