#include <iostream>  
#include <string>
using namespace std;
int main() {
	ios::sync_with_stdio(false);
	string word;
	while (1)
	{
		getline(cin, word);
		if (word == "")
			break;
		cout << word << '\n';
	}
	return 0;
}
