#include <iostream>
using namespace std;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int n, x, p;
	cin >> n >> x;
	for (int i = 0;i < n;i++) {
		cin >> p;
		if (p < x) {
			cout << p << " ";
		}
	}
	return 0;
}
