#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main(void)
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int N;
	cin >> N;
	int num_arr[1000];
	for (int i = 0;i < N;i++) {
		cin >> num_arr[i];

	}
	sort(num_arr,num_arr+N);
	for (int i = 0;i < 1000000;i++) {
		cout << num_arr[i] <<"\n";
	}
	return 0;
}
