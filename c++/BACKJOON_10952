#include <iostream>
using namespace std;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int a, b;
	int k = 0;
	while (k == 0) {
		cin >> a >> b;
		if (a != 0 || b != 0) {
			cout << a + b << endl;
		}
		else {
			k = 1;
		}
	}
	return 0;

}
